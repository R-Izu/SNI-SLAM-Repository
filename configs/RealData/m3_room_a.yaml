# 自動生成: tools/realdata/convert_stray.py
# scan_id: 707e94b9a5 / mode: anamorphic / generated: frame 1922
# 手で編集しないこと（再生成すると上書きされる）。
inherit_from: configs/Replica/replica.yaml
mapping:
  bound: [[0.162,7.312],[-1.472,1.744],[0.46,7.443]]
  marching_cubes_bound: [[0.162,7.312],[-1.472,1.744],[0.46,7.443]]
  # 実データは数千フレームあり、keyframe_every=4 だと keyframe の color/depth だけで
  # 6.1 GB になる（480 keyframe x 13.06 MB）。31 GB の実機では OOM するので
  # RAM に保持せず読み直す。数値は保持版と同一（src/Mapper.py: LazyKeyframe）。
  keyframe_store: reload
  # 中間メッシュは毎回**全 keyframe** を TSDF 積分するため、フレーム数の二乗で効いてくる。
  # 下流（precheck / Registration）が使うのは最終メッシュだけなので中間生成を止める。
  # final_mesh_semantic.ply は idx == n_img-1 で mesh_freq に関係なく生成される。
  mesh_freq: 100000
cam:
  H: 680
  W: 1200
  fx: 999.2449
  fy: 754.9851
  cx: 597.6908
  cy: 339.9412
  png_depth_scale: 1000.0
  crop_edge: 0
data:
  input_folder: data/realdata/m3_room_a
  output: output/RealData/m3_room_a/run1
